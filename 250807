# 학습 리포트 2025-08-07

## 오늘의 활동

- Dictionary의 인덱스를 직접 참조하려고 했지만, 불가능하다는 사실을 알게 됨
- 대신 LINQ의 `.ElementAt(i)` 메서드를 통해 인덱스로 접근할 수 있다는 걸 배움
- 하지만 이 방식은 내부적으로 순차 탐색을 하기 때문에 성능상 비효율적일 수 있다는 점도 함께 학습

### 새로 알게 된 점

- `Dictionary`는 기본적으로 순서를 보장하지 않는 자료구조이며, 인덱스 접근은 비권장
- `KeyValuePair` 리스트로 변환 후 `foreach`로 순회하는 방식이 성능 면에서 더 안정적
- 실무에서는 순서를 다루고 싶다면 `List<KeyValuePair<>>`를 활용하는 것이 좋음

### 느낀 점

- 자료구조의 특성과 사용 목적에 따라 적절한 순회 방식과 접근법을 택해야 한다
- LINQ는 간편하지만 무턱대고 쓰면 최적화에 악영향을 줄 수 있다는 걸 느꼈음
