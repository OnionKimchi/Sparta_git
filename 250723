# 학습리포트 2025-07-23

## 오늘의 활동

- 자료구조 기초 개념 학습
  - 모의 면접에서 자료구조 관련 질문에 제대로 답하지 못해 공부의 필요성을 절감
- 기존 프로젝트 코드의 리팩토링 진행
  - 마침 프로젝트 중 중복을 허용하지 않는 `List`를 다루는 코드가 있었기에 `HashSet`으로 자료구조를 리팩토링하여 중복 방지 로직 단순화

## 새로 알게 된 점 / 탐구한 내용

- `HashSet`은 기본적으로 중복을 허용하지 않는 자료구조이며, `Contains` 체크 없이도 `Add()`만으로 중복 제거 가능
  - 예: `if (!counts.Contains(x)) counts.Add(x);` → `counts.Add(x);` 만으로 충분
- 적절한 자료구조 선택은 코드의 가독성, 성능, 간결함에 모두 영향을 준다는 걸 체감

## 느낀 점

- 아무리 코드 구현 경험이 많아도 기초 이론 지식이 부족하면 발목을 잡힌다는 걸 느낌
- 리스트만 써도 다 구현이 되었기에 자료구조 관련 공부가 부족했음
- 실무에서도 맞는 자료구조를 쓰는 것만으로 버그를 줄이고 성능을 높일 수 있다는 점에서 기초 공부의 중요성을 실감

## 내일 학습 계획

- 다른 자료구조들도 예제를 통해 하나씩 정리해보고, 프로젝트에 더 활용해볼 방법을 탐색할 예정
